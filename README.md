# Credit-Scoring-Model
A Python-based Credit Scoring model built in Google Colab that utilizes Logistic Regression to predict borrower creditworthiness. The pipeline implements feature engineering (Debt-to-Income ratio), categorical encoding, feature scaling, and handles class imbalance using balanced class weights to evaluate risk metrics like ROC-AUC.
